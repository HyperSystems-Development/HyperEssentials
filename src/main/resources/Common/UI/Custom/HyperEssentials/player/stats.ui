$C = "../../Common.ui";
$S = "../shared/styles.ui";
$Nav = "../shared/nav_bar.ui";

$C.@PageOverlay {
  $Nav.@HyperEssentialsNavBar #NavBar {}

  $C.@Container {
    Anchor: (Width: 480, Height: 380);

    #Title {
      $C.@Title {
        @Text = "Stats";
      }
    }

    #Content {
      LayoutMode: Top;
      Padding: (Left: 15, Right: 15, Top: 10, Bottom: 10);

      // Player name header
      Label #PlayerName {
        Text: "Player";
        Style: (FontSize: 16, TextColor: #FFFFFF, RenderBold: true);
        Anchor: (Height: 28, Bottom: 6);
      }

      // Divider
      Group {
        Anchor: (Height: 1, Bottom: 10);
        Background: (Color: #FFAA00);
      }

      // Stats section
      Group #StatsList {
        LayoutMode: Top;
      }

      // Divider
      Group {
        Anchor: (Height: 1, Top: 10, Bottom: 10);
        Background: (Color: #2a3a4a);
      }

      // Status section header
      Label {
        Text: "Current Status";
        Style: (FontSize: 12, TextColor: #7c8b99, RenderBold: true);
        Anchor: (Height: 20, Bottom: 6);
      }

      Group #StatusList {
        LayoutMode: Top;
      }
    }
  }
}

$C.@BackButton {}
