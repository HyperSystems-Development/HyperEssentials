$C = "../../Common.ui";
$S = "../shared/styles.ui";
$Nav = "../shared/nav_bar.ui";

$C.@PageOverlay {
  $Nav.@HyperEssentialsNavBar #NavBar {}

  $C.@Container {
    Anchor: (Width: 480, Height: 380);

    #Title {
      $C.@Title {
        @Text = "Stats";
      }
    }

    #Content {
      LayoutMode: Top;
      Padding: (Left: 15, Right: 15, Top: 10, Bottom: 10);

      // Player name header
      Label #PlayerName {
        Text: "Player";
        Style: (FontSize: 16, TextColor: $S.@ColorWhite, RenderBold: true);
        Anchor: (Height: 28, Bottom: 6);
      }

      // Divider
      Group {
        Anchor: (Height: 1, Bottom: 10);
        Background: (Color: $S.@ColorGold);
      }

      // Stats section
      Group #StatsList {
        LayoutMode: Top;
      }

      // Divider
      Group {
        Anchor: (Height: 1, Top: 10, Bottom: 10);
        Background: (Color: $S.@ColorDivider);
      }

      // Status section header
      Label {
        Text: "Current Status";
        Style: (FontSize: 12, TextColor: $S.@ColorGray, RenderBold: true);
        Anchor: (Height: 20, Bottom: 6);
      }

      Group #StatusList {
        LayoutMode: Top;
      }
    }
  }
}

$C.@BackButton {}
