$C = "../../Common.ui";
$S = "../shared/styles.ui";
$Nav = "../shared/nav_bar.ui";

$C.@PageOverlay {
  $Nav.@HyperEssentialsNavBar #NavBar {}

  $C.@DecoratedContainer {
    Anchor: (Width: 580, Height: 440);

    #Title {
      $C.@Title {
        @Text = "Manage Kits";
      }
    }

    #Content {
      LayoutMode: Top;
      Padding: (Left: 15, Right: 15, Top: 10, Bottom: 10);

      // Header row: count + create button
      Group {
        Anchor: (Height: 28, Bottom: 6);
        LayoutMode: Left;

        Label #KitCount {
          Text: "0 kits";
          Style: (FontSize: 13, TextColor: #7c8b99, VerticalAlignment: Center);
          Anchor: (Width: 120);
        }

        Group { FlexWeight: 1; }

        $C.@TextField #KitNameInput {
          Anchor: (Width: 140, Height: 26);
          Style: (FontSize: 12, TextColor: #ffffff);
        }

        Group { Anchor: (Width: 6); }

        TextButton #CreateBtn {
          Text: "Create from Inventory";
          Style: $S.@GoldButtonStyle;
          Anchor: (Width: 160, Height: 26);
        }
      }

      // Divider
      Group {
        Anchor: (Height: 1, Bottom: 8);
        Background: (Color: #FFAA00);
      }

      // Scrollable kit list
      Group #KitListContainer {
        LayoutMode: TopScrolling;
        ScrollbarStyle: $C.@DefaultScrollbarStyle;
        FlexWeight: 1;

        Group #KitList {
          LayoutMode: Top;
        }
      }
    }
  }
}

$C.@BackButton {}
