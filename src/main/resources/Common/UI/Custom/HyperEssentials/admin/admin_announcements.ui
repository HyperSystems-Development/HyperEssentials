$C = "../../Common.ui";
$S = "../shared/styles.ui";
$Nav = "../shared/nav_bar.ui";

$C.@PageOverlay {
  $Nav.@HyperEssentialsNavBar #NavBar {}

  $C.@Container {
    Anchor: (Width: 580, Height: 440);

    #Title {
      $C.@Title {
        @Text = "Announcements";
      }
    }

    #Content {
      LayoutMode: Top;
      Padding: (Left: 15, Right: 15, Top: 10, Bottom: 10);

      // Info header
      Group {
        Anchor: (Height: 22, Bottom: 4);
        LayoutMode: Left;

        Label #MessageCount {
          Text: "0 messages";
          Style: (FontSize: 13, TextColor: $S.@ColorGray, VerticalAlignment: Center);
          Anchor: (Width: 150);
        }

        Group { FlexWeight: 1; }

        Label #IntervalLabel {
          Text: "";
          Style: (FontSize: 11, TextColor: $S.@ColorGray, VerticalAlignment: Center);
          Anchor: (Width: 200);
        }
      }

      // Mode label
      Group {
        Anchor: (Height: 18, Bottom: 6);

        Label #ModeLabel {
          Text: "";
          Style: (FontSize: 11, TextColor: #FFAA00);
        }
      }

      // Divider
      Group {
        Anchor: (Height: 1, Bottom: 8);
        Background: (Color: $S.@ColorGold);
      }

      // Info note
      Label {
        Text: "Messages are managed in announcements.json config file.";
        Style: (FontSize: 10, TextColor: $S.@ColorGray);
        Anchor: (Height: 16, Bottom: 8);
      }

      // Scrollable message list
      Group #MessageListContainer {
        LayoutMode: TopScrolling;
        ScrollbarStyle: $C.@DefaultScrollbarStyle;
        FlexWeight: 1;

        Group #MessageList {
          LayoutMode: Top;
        }
      }
    }
  }
}

$C.@BackButton {}
